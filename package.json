{
  "name": "dochadzkovy-system",
  "version": "1.0.0",
  "description": "Dochádzka Pro - Multi-tenant attendance tracking system",
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
